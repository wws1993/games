易手办咨询 App

- 第一步～第三步已完成（见历次记录）
- 第四步完成：微信支付页/结果页、优惠券与会员展示、订单评价与发票申请、首页搜索过滤、资讯详情、服务费用计算器
- 首页顶区滚动优化：顶栏固定不参与滚动，内容区独立滚动；滚动后顶区收缩为品牌 LOGO 紧凑样式
- 首页本地资讯增至 6 条，内容区底部增加 TabBar 避让间距
- 首页服务分类：恢复等分四列布局，图标 54px + 分层阴影立体化（去掉 20vw）
- 首页底部间距：去掉 TabBar 留白重复叠加，只保留滚动区内一层避让
- 首页搜索框与服务分类横向与顶栏对齐（搜索全宽、分类 space-between）
- 首页轮播 Banner 与顶栏同宽（去掉左右外扩）
- 品牌更名：企民通 → 易手办（首页、Mock 文案、配置与文档同步）
- 首页顶栏收起态：左右留 12px 内边距
- 全局隐藏滚动条（保留滚动能力）
- 首页 Banner 指示点移至右下角玻璃胶囊内，激活态进度动画
- 二级页 NavBar 返回按钮左边距 +12px
- 新增 Select 组件（自定义下拉，毛玻璃样式），费用估算页替换原生 select
- 二级页（含 business/服务分类）顶栏固定，内容区独立滚动
- Button 增加 sm 尺寸；优惠券页按钮缩小字号并保留内边距
- 我的页新增「关于我们」入口与详情页
- 全站滚动方案统一：Tab 页 PageLayout fill（标题固定 + 内容区滚动）；二级页 SubLayout 顶栏固定 + scrollBody 滚动
- 底部操作统一 ActionBar：一行 flex 贴底固定，含 safe-area；PageLayout footer 接入
- 主按钮异形圆角（22/22/22/8）+ 多层蓝色投影；服务详情/订单详情/支付/评价/发票等页已迁移
- 修复登录页中文文案乱码（源文件 UTF-8 损坏为 ????）
- 咨询页输入框被 TabBar 遮挡：ChatView 底部增加 TabBar 避让间距
- 全站排版间距优化：统一 spacing 层级（tight/inline/block/group/section），模块间 24px、组内 16px、标签 6px
- 咨询页输入框：单行垂直居中、多行自动增高（最多 4 行）、移动端键盘避让与 enterKeyHint
- 移除首页 FluidBackground 动画，全站统一 body 20° 渐变底
- 首页 Banner 换为 3 张配图（商务咨询/创业办公/医疗健康），本地 public/img/home/banner/
- Banner 手动滑动/点指示器后重置自动播放计时，避免与自动轮播冲突
